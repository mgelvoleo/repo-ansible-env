---

- hosts: all
  become: true
  tasks:

  - name: install apache2 packages
    apt:
      name: apache2  
      state: absent
  
  - name: install php support for apache
    apt:
      name: libapache2-mod-php
      state: remove